"""پنل وب مدیریت.

پنل داخل همان پروسه‌ی ربات بالا می‌آید، نه به‌عنوان یک برنامه‌ی جدا. دلیلش
یکی نیست، چند تاست:

- دیتابیس SQLite است و دو پروسه که همزمان رویش بنویسند سر قفل به هم
  می‌خورند. یک پروسه یعنی یک موتور SQLAlchemy و هیچ قفلی.
- منطق کار (تأیید رسید، فعال کردن اشتراک، محاسبه‌ی ارتقا) همین حالا در
  `telkap/services/` نوشته و تست شده است. پنل همان‌ها را صدا می‌زند، نه
  اینکه دوباره‌شان بنویسد — دو پیاده‌سازی از یک قاعده، دیر یا زود از هم
  فاصله می‌گیرند و آن‌وقت پول آدم‌ها وسط است.
- برای استقرار چیزی اضافه نمی‌شود: همان `git pull` و همان ری‌استارت سرویس.

پیش‌فرض خاموش است. تا `WEB_ENABLED=true` نشود هیچ پورتی باز نمی‌شود.
"""
from __future__ import annotations

from telkap.web.server import start_panel, stop_panel

__all__ = ["start_panel", "stop_panel"]
